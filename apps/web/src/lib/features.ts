// Global feature switches — NOT plan-gated. These lock a feature for EVERY plan (free → Premium), e.g.
// something that isn't ready to launch yet. Flip a flag to false to turn the feature back on everywhere.

// Achievements & Rewards visibility. Set to true to show them locked ("Coming soon") for all plans; false
// unlocks them everywhere (page, sidebar entry, and the reward/achievement widgets on the Home dashboard).
export const REWARDS_LOCKED = false;

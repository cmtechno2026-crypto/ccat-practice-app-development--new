// Global feature switches — NOT plan-gated. These lock a feature for EVERY plan (free → Premium), e.g.
// something that isn't ready to launch yet. Flip a flag to false to turn the feature back on everywhere.

// Achievements & Rewards are shown but locked ("Coming soon") for all plans until launch. This governs the
// Achievements/Rewards page, its sidebar entry, and the reward/achievement widgets on the Home dashboard.
export const REWARDS_LOCKED = true;

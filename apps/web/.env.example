# Copy to .env.local. The ONLY config the web client needs: the gateway base URL.
# No Supabase key, no secrets, no service_role — the client talks only to the gateway.
VITE_GATEWAY_URL=http://localhost:8080

// Book Store was removed (replaced by Teachers). This stub remains only so no dangling import breaks
// the build; it is not routed or referenced anywhere and is safe to delete.
export function Books() {
  return null;
}
